import { PreparationScreen } from '@/pages/preparation';
import { Screen } from '@/shared/ui';

export default function Preparation() {
  return (
    <Screen>
      <PreparationScreen />
    </Screen>
  );
}
